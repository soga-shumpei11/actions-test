name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: test
        run: |
          echo "test"
      #   run: |

      #     echo "test"
      #     diff -u <(echo "test") <(echo "test") | STATUS=$?
      #     echo $STATUS
      # - name: test
      #   run: |
      #     echo "test"
      #     diff -u <(echo "test") <(echo "test") | STATUS=$?
      #     echo $STATUS
      #   - name: test
      #   run: |
      #     echo "test"
      #     diff notfound.txt notfounds.txt | STATUS=$?
      #     echo $STATUS
      